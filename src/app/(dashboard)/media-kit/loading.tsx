import { PageSkeleton } from '@/components/shared/page-skeleton'

export default function Loading() {
  return <PageSkeleton title="Media Kit" subtitle="A professional pitch page brands can view and download" rows={2} />
}
