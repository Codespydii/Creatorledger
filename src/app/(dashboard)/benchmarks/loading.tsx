import { PageSkeleton } from '@/components/shared/page-skeleton'

export default function Loading() {
  return <PageSkeleton title="Rate Benchmarks" subtitle="What creators like you actually charge" rows={3} />
}
